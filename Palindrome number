class Solution {
    public boolean isPalindrome(int x) {
        if (x < 0)
        {
            return false;
        }
        int res = 0, y = x;
        while (y != 0)
        {
         res = 10*res + y % 10;
         y = y/10;
         }
        return (x == res);
    }
}
